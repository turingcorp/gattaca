import Foundation

extension MHome
{
    enum Status
    {
        case new
        case loading
        case sync
        case loaded
        case authLocation
        case waitingLocation
        case syncLocation
        case ready
    }
}
