import UIKit

struct MMenuItemSettings:MMenuItemProtocol
{
 //   var signature: ModelSignature<VHome>

    let order:MMenu.Order
    let icon:UIImage = #imageLiteral(resourceName: "assetGenericSettings")
}
