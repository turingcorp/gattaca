import Foundation

extension MSession
{
    //MARK: internal
    
    func clearDifferentLocations(country:String)
    {
    }
    
    func saveNewLocation(
        country:String,
        completion:@escaping(() -> ()))
    {
        
    }
    
    func syncNewLocation(
        latitude:Double,
        longitude:Double,
        country:String,
        completion:@escaping(() -> ()))
    {
        
    }
}
